import React from 'react';
import './Bookmark.css'

const Bookmark = ({ blog }) => {
    const { title } = blog;
    
    return (
        <div>
            
        </div>
    );
};

export default Bookmark;